# Optional SMTP configuration (values can be overridden by CLI/gui)

# For Gmail:
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=465

# Who you send as (often same as username)
FROM_EMAIL=your_email@gmail.com
SMTP_USERNAME=your_email@gmail.com

# App-specific password (NOT your Google login password)
# SMTP_PASSWORD=your_app_password

# Optional defaults for content
# SUBJECT=Hello from the app
# BODY=This is a test message

# Gmail OAuth (Gmail API) config
# Path to OAuth client secrets JSON you download from Google Cloud Console
# GOOGLE_CLIENT_SECRETS=google_client_secret.json
# Path where access/refresh tokens will be stored after first consent
# GOOGLE_TOKEN_FILE=token.json
