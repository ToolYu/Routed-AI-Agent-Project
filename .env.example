# SMTP (optional – only needed if using Gmail App Password)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=465
SMTP_USERNAME=your_email@gmail.com
# SMTP_PASSWORD=your_app_password
FROM_EMAIL=your_email@gmail.com

# Gmail OAuth configuration
GOOGLE_CLIENT_SECRETS=google_client_secret.json
GOOGLE_TOKEN_FILE=token.json

# Optional defaults
# INTENT_TO_CONNECT="networking purposes"
# SEND_TO=someone@example.com

# Language model settings
LLM_PROVIDER=groq
GROQ_MODEL_VERSION=llama-3.3-70b-versatile
# LLM_PROVIDER=openai
# OPENAI_MODEL=gpt-4o-mini

# API keys (leave blank; each developer fills locally)
GROQ_API_KEY=
OPENAI_API_KEY=
AZURE_OPENAI_API_KEY=
